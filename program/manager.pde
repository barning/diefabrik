class Manager { 

  void isPayday(){ 
    if (second()==5) {
      println("payday");
      klaus.myPocket(sliderMoney);
      peter.myPocket(sliderMoney);
      hans.myPocket(sliderMoney);
      dieter.myPocket(sliderMoney);
      gerhard.myPocket(sliderMoney);
    }
  }
  void advise(){
    peter.work();
    klaus.work();
    hans.work();
    dieter.work();
    gerhard.work();
  } 
};
