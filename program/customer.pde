class Customer {	
  void getProduct(int p){
    if (worker1.done||worker2.done||worker3.done||worker4.done||worker5.done||worker6.done||worker7.done||worker8.done||worker9.done){
      money+=p;
    }
  }	
};
